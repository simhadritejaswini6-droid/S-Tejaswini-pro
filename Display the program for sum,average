#include <stdio.h>
int main()
{
    int a,b;
    int sum,average;
    printf("Enter any two integers:");
    scanf("%d %d",&a, &b);
    sum=a+b;
    average=sum/2;
    printf("Sum =%d and Average =%d\n",sum,average);
    return 0;
}
