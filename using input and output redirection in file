#include <stdio.h>
int main() {
    char name[50];
    scanf("%s", name);      // Reads input normally
    printf("Name: %s\n", name);   // Prints output normally
    return 0;
}
Compile:
gcc redirect.c -o a.out
Run with Input Redirection
a.out < input.txt
Run with Output Redirection
a.out > output.txt

